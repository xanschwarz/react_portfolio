import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
// Check the following works, may need to relocate.
import './index.css';

ReactDOM.render(<App />, document.getElementById('root'));
